---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title} showReadingProgress={true}>
  <div class="max-w-[75ch] mx-auto px-6 py-12">
    <slot />
  </div>
</BaseLayout>
